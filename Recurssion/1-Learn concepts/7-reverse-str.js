function reverseStr(str, i) {
    if (i >= str.length) {
        return;
    }
    reverseStr(str, index + 1);
    console.log(str[i])
}

//................................................................................................
