var merge = function(arr) {
    arr.sort((a,b)=>a[0]-b[0]);

    console.log(arr);
    let ans =[arr[0]];
    for(let i=0; i<arr.length ; i++){
        let lastEnd = ans[arr.length-1];
        // if(lastEnd >)
    }
};

intervals = [[1,3],[8,10],[15,18],[2,6],[2,4]];

merge(intervals);
